name: tim
description: "A new Flutter plugin project."
version: 0.0.2
homepage:

environment:
  sdk: ^3.9.2
  flutter: '>=3.3.0'

dependencies:
  flutter:
    sdk: flutter
  plugin_platform_interface: ^2.0.2
  flutter_blue_plus: ^1.35.5
  flutter_rust_bridge: 2.11.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  plugin:
    platforms:
      android:
        package: com.mobius.tim
        pluginClass: TimPlugin
      ios:
        pluginClass: TimPlugin
      macos:
        pluginClass: TimPlugin